#include "main.h"

/**
 * int _isupper - checks whether its an upper case character .
 * @c input character .
 * Returns 1 if c is uppercase,0 in other case .
 */
int _isupper(int c);
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
